import React, { Component } from 'react';
import Modal from '../Modal'



const UserSequences = () => {
    const buttonText = "Sequences";
    return <Modal buttonText={buttonText} title="Saved sequences" body="here are your sequences" />
}

export default UserSequences;

